import { ResponseRestaurant } from './response-restaurant';

describe('ResponseRestaurant', () => {
  it('should create an instance', () => {
    expect(new ResponseRestaurant()).toBeTruthy();
  });
});
